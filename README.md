# lottodraw.kr

로또 6/45 번호 생성기와 전 회차 통계 사이트. 주소는 <https://www.lottodraw.kr>.

이 문서는 **작업 인수인계서**다. 회사 PC, 집 PC, Claude 채팅, VS Code 등 어디서
작업하든 이 파일 하나만 읽으면 "지금 어디까지 왔고, 다음에 뭘 해야 하는지"를 알 수
있도록 쓴다. 작업을 마칠 때마다 맨 아래 **진행 기록**에 한 줄씩 추가한다.

> **Claude에게:** 작업을 시작하기 전에 이 문서를 끝까지 읽을 것. 작업이 끝나면
> "진행 기록"과 "할 일"을 갱신하는 것까지가 작업이다.

---

## 1. 지금 상태 한눈에 보기 (2026-09-22 기준)

| 항목 | 상태 |
|---|---|
| 사이트 운영 | ✅ 정상 (GitHub Pages, www.lottodraw.kr) |
| 매주 당첨번호 자동 갱신 | ✅ 정상 (최근 1242회차까지 반영, 2026-09-19) |
| 무료 기능 (생성기, 통계, 당첨번호, 실수령액, TOP 50) | ✅ 공개 중 |
| 유료 기능 (5개월 통계, 상세 분석 4종) | ⚠️ **코드는 완성, 결제 연결 전**. 지금 방문자에게는 "결제 준비 중"으로 보임 |
| Polar 결제 연동 | ❌ **가입·설정 필요** → [4. 다음 할 일](#4-다음-할-일-polar-결제-연결) |

---

## 2. 사이트 구성

### 페이지

| 파일 | 내용 |
|---|---|
| `index.html` | 홈. 번호 생성기 + 통계 요약 + 잠긴 상세 분석 4종 |
| `draws.html` | 전 회차 당첨번호 목록 |
| `round/1.html` … | 회차마다 한 페이지 (검색 유입용) |
| `statistics.html` | **유료** 최근 5개월(22회차) 통계. 결제 페이지 겸용 |
| `statistics-*.html` (12개) | 검색용 정적 통계 페이지 (빈도, 홀짝, 합계, 쌍 등) |
| `probability.html` | 당첨 확률 설명 |
| `tax.html` | 당첨금 실수령액 계산기 |
| `top-prize.html` | 역대 1등 당첨금 TOP 50 |
| `about.html` `contact.html` `privacy.html` `terms.html` | 소개, 문의, 개인정보처리방침, 이용약관 |

### 데이터와 코드

| 파일 | 역할 |
|---|---|
| `lotto-data.json` | **전 회차 당첨 기록 원본.** 모든 페이지의 숫자가 여기서 나온다 |
| `js/lotto-stats.js` | 통계 계산 |
| `js/stats-render.js` `js/insights.js` `js/details.js` | 통계를 화면에 그리기 |
| `js/license.js` | 이용권 키가 유효한지 Polar에 물어보고 판정 |
| `js/premium.js` | 결제 페이지의 잠금/해제 화면 전환 |
| `js/premium-config.js` | **Polar 설정값을 넣는 곳** (지금은 비어 있음) |
| `tools/update-lotto-data.js` | 동행복권에서 새 회차 받아오기 |
| `tools/build-static-stats.js` | 통계 페이지, 회차 페이지, 사이트맵 다시 만들기 |
| `.github/workflows/update-lotto-data.yml` | 위 두 도구를 매주 자동 실행하는 예약 작업 |

### 기타 파일

- `CNAME`: 사이트 주소(www.lottodraw.kr)를 GitHub Pages에 알려주는 파일
- `ads.txt`: 구글 애드센스 광고 인증 파일
- `naver….html`: 네이버 서치어드바이저 소유 확인 파일. **지우면 안 된다**
- `robots.txt` `sitemap.xml`: 검색엔진용 안내 파일

---

## 3. 자동 갱신은 어떻게 돌아가나 (GitHub Actions)

> **GitHub Actions란?** GitHub가 빌려주는 임시 컴퓨터가 정해진 시간에 저장소의
> 대본(`.github/workflows/*.yml`)대로 명령을 실행해 주는 기능이다. 내 컴퓨터가
> 꺼져 있어도 돌아간다.

```
① 예약 시간 도달
   · 일요일 03:10 (토요일 밤 추첨 결과 수집)
   · 월요일 09:10 (1등 당첨금이 늦게 확정되는 경우 보정)
        ↓
② 동행복권에서 새 회차를 받고, 최근 3회차를 다시 확인
   → 바뀐 게 없으면 여기서 끝
        ↓
③ 통계 페이지, 회차 페이지, 사이트맵을 새 데이터로 다시 생성
        ↓
④ "github-actions[bot]" 이름으로 저장소에 저장(커밋)
        ↓
⑤ GitHub Pages에 재배포 요청 → 사이트 반영
```

- **실제 실행은 예정보다 2~5시간 늦는 게 보통이다.** GitHub 예약 실행은 정시를
  보장하지 않는다. 일요일 오전 중에 반영되면 정상이다.
- **동행복권 데이터를 브라우저에서 직접 불러오지 않는 이유:** 동행복권 서버가
  다른 사이트에서의 호출을 막는다(CORS라는 브라우저 보안 규칙). 그래서 GitHub의
  임시 컴퓨터가 대신 받아서 `lotto-data.json` 파일로 저장해 두고, 사이트는 그
  파일만 읽는다.
- **실행 기록 보는 법:** GitHub 저장소 → 상단 **Actions** 탭. 초록 체크는 성공,
  빨간 X는 실패다.
- **수동 실행:** Actions 탭 → 왼쪽 `update-lotto-data` → **Run workflow** 버튼.
- **주의:** 예약 실행은 저장소의 기본 브랜치(`html`)에 있는 대본만 돈다.

---

## 4. 다음 할 일: Polar 결제 연결

> **Polar(polar.sh)란?** 디지털 상품 결제 대행 서비스다. 카드 결제, 해외 세금
> 처리, 영수증, 구독 해지까지 대신 해준다. 결제하면 구매자에게 **라이선스 키**
> (이용권 번호)를 자동으로 보내고, 사이트는 그 키가 유효한지 Polar에 물어봐서
> 잠금을 푼다.

**사이트 코드는 이미 완성돼 있다.** `js/premium-config.js`의 빈칸만 채우면 결제
버튼이 살아난다.

### 판매 상품 (결제 페이지에 이미 적혀 있는 가격)

| 코드 이름 | 상품 | 가격 | 키 유효기간 |
|---|---|---|---|
| `week` | 1주 이용권 | $2 | 7일 |
| `sub` | 월 구독 (자동 갱신) | $4/월 | 해지할 때까지 |
| `month` | 1개월 이용권 | $5 | 1개월 |
| `lifetime` | 평생 이용권 | $10 | 없음 |

### 체크리스트

- [ ] **1. polar.sh 가입, 조직(Organization) 만들기**
- [ ] **2. 정산 계좌 연결.** 한국 개인/사업자 계좌로 정산이 되는지 가입 단계에서
      먼저 확인한다. 안 되면 여기서 멈추고 다른 결제대행사를 검토해야 한다
- [ ] **3. 상품 4개 만들기** (위 표대로. `sub`만 반복 결제 상품으로)
- [ ] **4. 각 상품에 License Key 혜택(Benefit) 붙이기.** 유효기간을 위 표대로 설정한다
- [ ] **5. 각 상품의 Checkout Link(결제 링크) 만들기**
- [ ] **6. 값 모으기** → 아래 "넣을 값" 표 참고
- [ ] **7. 먼저 샌드박스(테스트 모드)로 시험.** `apiBase`를
      `'https://sandbox-api.polar.sh'`로 바꾸고 샌드박스 값으로 테스트 결제
      → 받은 키를 결제 페이지에 넣어 잠금이 풀리는지 확인
- [ ] **8. 실제 값으로 교체**, `apiBase`를 `'https://api.polar.sh'`로 되돌리고 커밋
- [ ] **9. 실제로 $2 한 번 결제해서** 키 발급 → 잠금 해제 → 환불 → 다시 잠기는지 확인

### 넣을 값

| 넣을 곳 (`js/premium-config.js`) | Polar 어디서 |
|---|---|
| `organizationId` | Settings → General |
| `portalUrl` | `https://polar.sh/<조직 주소>/portal` |
| `plans.week.checkoutUrl` 등 | 각 상품의 Checkout Link 주소 |
| `plans.week.benefitId` 등 | 각 상품에 붙인 License Key 혜택의 ID |

### 꼭 지킬 것

- **`premium-config.js`에는 공개돼도 되는 값만 넣는다.** 이 파일은 누구나 볼 수
  있다. Polar의 **API 토큰(비밀 키)은 절대 넣지 않는다.** 위 표의 값들은 전부
  공개돼도 되는 값이다.
- 잠금은 **편의 잠금**이다. 원본 데이터가 공개돼 있어서, 마음먹은 사람이 직접
  계산하는 것까지 막지는 못한다. 이건 알고 선택한 구조다.
- 결제대행사 장애 때 이미 결제한 사람이 막히지 않도록, 확인한 키는 12시간 동안
  다시 묻지 않고 연결 실패 시 72시간까지 봐준다(`revalidateHours`, `graceHours`).

---

## 5. 작업 방식과 주의사항

### 브랜치

> **브랜치란?** 같은 저장소 안의 별도 작업 공간이다. 실제 사이트는 `html`
> 브랜치 하나에서 나온다.

- **`html`이 실제 사이트다.** 여기에 올라간 것이 곧바로 배포된다.
- Claude(웹)가 작업하면 `claude/…` 브랜치에서 고친 뒤 **Pull Request**(변경
  제안서)를 만든다. GitHub에서 **Merge** 버튼을 눌러야 `html`에 합쳐진다.

### ⚠️ 옛 파일로 덮어쓰지 말 것

**봇이 매주 `html` 브랜치에 직접 저장한다.** 그래서 GitHub에 있는 파일이 항상
내 컴퓨터(Dropbox 날짜 폴더)의 파일보다 최신이다.

로컬에 있던 옛 파일을 GitHub 웹 업로드로 올리면 **봇이 넣은 최신 회차가 이전
상태로 되돌아간다.** 에러도 나지 않는다. 특히 조심할 파일:

`lotto-data.json` · `index.html` · `draws.html` · `statistics-*.html` ·
`sitemap.xml` · `round/` · `tax.html` · `top-prize.html` · `probability.html`

**수정할 파일은 항상 GitHub에서 최신본을 받아서 고친다.**

### 문구 규칙

- **"당첨 확률을 올려준다"는 뉘앙스의 문구는 쓰지 않는다.** 로또는 모든 조합의
  확률이 같다. "확률 상승", "적중률", "추천 번호", "AI 예측" 금지.
- 통계는 "지나간 기록이 이랬다"까지만 보여준다. 예측으로 읽히게 쓰지 않는다.

### 참고: 옛 계획 문서

Dropbox `260914 클로드` 폴더의 README와 CLAUDE.md는 **실제로 적용되지 않은 초기
계획**이다(Python, `templates/` 폴더, Cloudflare 배포 기준). 지금 구조와 다르니
그 문서의 파일 경로나 배포 설명을 따르지 말 것. 이 README가 기준이다.

---

## 6. 진행 기록

새 작업은 맨 위에 추가한다. 형식: `날짜 | 어디서(회사/집, 채팅/VS Code) | 한 일`

| 날짜 | 어디서 | 한 일 |
|---|---|---|
| 2026-09-24 | Claude 웹 | 개별 통계 페이지에 정규분포 그래프 추가: `statistics-sum.html`(합계 히스토그램+이론 곡선), `statistics-frequency.html`(번호 공 분포+σ 열). 홈은 그대로 |
| 2026-09-22 | 집, VS Code | README 작성 (인수인계용). Polar 연결 체크리스트 정리 |
| 2026-09-21 | Claude 웹 | 홈 데이터세트 구조화 데이터 보완 (PR #5) |
| 2026-09-19 | 자동(봇) | 1242회차 반영 |
| 2026-09-16 | Claude | 월 $4 구독 상품 추가, 상세 분석 4종 홈에 잠금 처리, 검색용 정적 통계 페이지 12종 + 회차별 페이지 생성, 검색 키워드 개편, 라이브리 댓글 제거 |
| 2026-09-15 | Claude 웹 | 홈을 세로 3구간으로 재구성, 통계 3종 추가, ABOUT·실수령액·TOP 50 정리, 개인정보처리방침·이용약관·문의 페이지 정비 (PR #1~#4) |
| 2026-09-14 | Claude | 구조 개편 계획 수립 (`260914 클로드` 폴더) |
| 2026-05-03 | — | 통계 10종, 메뉴바 수정, 회차 엑셀 → JSON 변환 |
| 2026-02-22 | — | 실수령액 계산, 역대 당첨금액표 |
| 2026-02-08 | — | 통계 페이지 |
| 2026-02-03 ~ 07 | — | 구글 애드센스 요건 대응 (필수 페이지 추가) |
| 2025-12-30 | — | 시작 |

## 7. 할 일

- [ ] **Polar 결제 연결** ([4번](#4-다음-할-일-polar-결제-연결) 체크리스트)
- [ ] 결제 연결 후 실제 결제 → 해제 → 환불 흐름 확인
- [ ] 이용약관(`terms.html`)의 환불 기준이 Polar 설정과 일치하는지 확인
